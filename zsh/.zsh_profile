export PYTHONDONTWRITEBYTECODE=1
export XDG_CONFIG_HOME=$HOME/.config
VIM="nvim"

alias ll="ls -la"

bindkey -s ^f "tmux-sessionizer\n"

path+=("$HOME/.local/scripts/")
export PATH

keychain id_rsa && source ~/.keychain/`uname -n`-sh
